var item = document.getElementById("cart_item-" + "<%= @cart_item.product_id %>");
$(item).find("#quantity").text("<%= @cart_item.quantity %>");
$("#total_price").html("$" + "<%= current_cart.total_price %>")